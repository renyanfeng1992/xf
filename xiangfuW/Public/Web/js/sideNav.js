/** * Created by lenovo on 2017/2/6. */$(document).ready(function () {    var url = "http://www.sino-bridge.com";    //获取id    function GetQueryString(name) {        var reg = new RegExp("(^|&)" + name + "=([^&]*)(&|$)", "i");        var r = window.location.search.substr(1).match(reg);        if (r != null) return (r[2]);        return null;    }    var id = GetQueryString("navid");    var ID = GetQueryString("id");    //请求页面数据    $.ajax({        type: "post",        url: "http://153.37.149.247:80/sinoWeb/navigation/findNavByArticle",        dataType: "json",        data: {id: id},        success: function (data) {            var obj = eval(data);                  var secSubMenu = obj.data.list.subMenu;            var str = "";            if (secSubMenu) {                for (var i = 0; i < secSubMenu.length; i++) {                    str += ' <h3 class="menu_head current">' + secSubMenu[i].navigationName + '</h3>'                        + '<div  class="menu_body" id="'+i+'">';                    if(secSubMenu[i].list){                        for (var j = 0; j < secSubMenu[i].list.length; j++) {                            str += '<a class="sidebtn" id="' + secSubMenu[i].list[j].aId + '" _navid="'+ secSubMenu[i].list[j].navId+'">' + secSubMenu[i].list[j].title + '</a>'                        }                    }                    str += '</div>'                }                            }                        $("#firstpane").html(str);            $("#" + ID).addClass("addcolor")            $(".menu_body").hide()            $("#" + ID).parent('.menu_body').show();                 $("#firstpane h3.menu_head").click(function () {                $(this).addClass("current").next("div.menu_body").slideToggle(300).siblings("div.menu_body").slideUp("slow");                $(this).siblings().removeClass("current");            });                        //左侧菜单文章点击跳转            $(".sidebtn").click(function () {                var id = $(this).attr("id");                var navid = $(this).attr("_navid");                window.location.href = "../articleDetail/articleDetail.html?id=" + id + '&&navid=' + navid;            })        }    });});